/* FantAbba 2.0 compatibility shim.
   The legacy builder has been retired. The unified decision engine lives in app.js.
   Kept as an empty shim so old cached pages cannot attach a second click handler. */
window.FANTABBA_LEGACY_BUILDER_RETIRED=true;
